pcm.opennow_test_output {
    type null
    hint {
        show on
        description "OpenNOW isolated test output"
        ioid "Output"
    }
}

pcm.opennow_test_failing_output {
    type file
    slave.pcm "null"
    file "/dev/full"
    format "raw"
    hint {
        show on
        description "OpenNOW test output that fails once its buffer fills"
        ioid "Output"
    }
}

pcm.null {
    type null
}
